local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

-- 🔹 Kết nối slider UI (Đổi đường dẫn nếu cần)
local UserSlider = game.Players.LocalPlayer.PlayerGui.ScreenGui.Slider  
local hitboxSize = UserSlider.Value  

local function createESP(character, player)
    if not character then return end
    repeat wait() until character:FindFirstChild("HumanoidRootPart")

    local root = character:FindFirstChild("HumanoidRootPart")
    local head = character:FindFirstChild("Head")
    if not root or not head then return end

    -- 🟢 Chọn màu theo team
    local color = (player.Team == Players.LocalPlayer.Team) and Color3.new(0, 0, 1) or Color3.new(1, 0, 0)

    -- 📌 Name ESP
    local billboard = Instance.new("BillboardGui", game.CoreGui)
    billboard.Adornee = head
    billboard.Size = UDim2.new(0, 100, 0, 30)
    billboard.StudsOffset = Vector3.new(0, 2, 0)
    billboard.AlwaysOnTop = true

    local textLabel = Instance.new("TextLabel", billboard)
    textLabel.Size = UDim2.new(1, 0, 1, 0)
    textLabel.BackgroundTransparency = 1
    textLabel.Text = player.Name
    textLabel.TextColor3 = color
    textLabel.TextStrokeTransparency = 0.5
    textLabel.Font = Enum.Font.SourceSansBold
    textLabel.TextSize = 14

    -- 📌 Box ESP (Highlight)
    local highlight = Instance.new("Highlight", character)
    highlight.FillTransparency = 0.5
    highlight.OutlineColor = color
    highlight.OutlineTransparency = 0

    -- 📌 Hitbox
    local hitbox = Instance.new("Part", character)
    hitbox.Size = Vector3.new(hitboxSize, hitboxSize, hitboxSize)
    hitbox.Transparency = 0.7
    hitbox.CanCollide = false
    hitbox.Anchored = true
    hitbox.Color = color
    hitbox.Material = Enum.Material.ForceField

    -- 📌 Cập nhật hitbox khi slider thay đổi
    local updateConnection
    updateConnection = RunService.RenderStepped:Connect(function()
        if root then
            hitbox.Position = root.Position
            hitbox.Size = Vector3.new(hitboxSize, hitboxSize, hitboxSize)
        else
            updateConnection:Disconnect()
        end
    end)

    -- 📌 Xóa ESP khi nhân vật chết
    character.Destroying:Connect(function()
        billboard:Destroy()
        highlight:Destroy()
        hitbox:Destroy()
    end)
end

-- 📌 Cập nhật hitbox khi kéo slider
UserSlider.Changed:Connect(function(value)
    hitboxSize = value
end)

-- 📌 Áp dụng cho tất cả người chơi
for _, player in pairs(Players:GetPlayers()) do
    if player.Character then createESP(player.Character, player) end
    player.CharacterAdded:Connect(function(character)
        createESP(character, player)
    end)
end

-- 📌 Áp dụng cho người chơi mới vào
Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function(character)
        createESP(character, player)
    end)
end)
